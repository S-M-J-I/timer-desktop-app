package project.controller;

import project.views.ViewFactory;

public class AlertController extends BaseController{
    public AlertController(ViewFactory viewFactory, String fxmlName) {
        super(viewFactory, fxmlName);
    }

}
